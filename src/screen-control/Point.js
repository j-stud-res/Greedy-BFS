export default class Point {
    /**
     * 
     * @param {number} x - Value of x
     * @param {number} y - Value of y
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}