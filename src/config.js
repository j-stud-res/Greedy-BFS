/**
 * @typedef GridConfig
 * @type {object}
 * @property {number} gridWidth - width of the grid inside the canvas.
 * @property {number} gridHeight - height of the grid inside the canvas.
 * @property {number} cellsWidthCount - the number of cells per row.
 * @property {number} cellsHeightCount - the number of rows of cells.
 /


/**
 * @exports
 * @constant
 * @type GridConfig
 */
export const GridConfig = {
    gridWidth: 500,
    gridHeight: 500,
    cellsWidthCount: 50,
    cellsHeightCount: 50
}